---
kind: Pod
apiVersion: v1
metadata:
      name: testpod
      annotations:
         description: hamara phela test code hai :))
spec:
    containers:
      - name: c00
        image: ubuntu
        command: ["/bin/bash", "-c", "while true; do echo Hello-Bhupi; sleep 5 ; done"]
    restartPolicy: Never        # default to always
